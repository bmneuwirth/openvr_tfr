Build instructions (requires CMake):

> cd samples
> mkdir build
> cd build
> cmake ..

Open the generated openvr_samples.sln file in samples/build and run the project. 
Functions of interest are RenderStereoTargets and RenderFrame